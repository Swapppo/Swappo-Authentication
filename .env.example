# Authentication Microservice Configuration

# JWT Secret Keys (Generate new secrets for production!)
# Use: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-secret-key-here-generate-a-new-one
REFRESH_SECRET_KEY=your-refresh-secret-key-here-generate-a-new-one

# Token Expiration
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# Database Configuration (for production)
DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# CORS Settings
ALLOWED_ORIGINS=https://yourmobileapp.com,https://yourwebapp.com

# Server Settings
HOST=0.0.0.0
PORT=8000

# Environment
ENVIRONMENT=development
